import {Check,ChevronDown} from "lucide-react";import {useEffect,useId,useMemo,useRef,useState} from "react";interface Props{value:string;options:string[];placeholder?:string;onChange:(value:string)=>void}export default function ComboInput({value,options,placeholder,onChange}:Props){const[open,setOpen]=useState(false);const[filter,setFilter]=useState<string|null>(null);const root=useRef<HTMLDivElement>(null);const listId=useId();useEffect(()=>{if(!open)return;const close=(event:PointerEvent)=>{if(!root.current?.contains(event.target as Node))setOpen(false)};document.addEventListener("pointerdown",close);return()=>document.removeEventListener("pointerdown",close)},[open]);const visible=useMemo(()=>{const unique=[...new Set(options.map(item=>item.trim()).filter(Boolean))];if(filter===null||!filter.trim())return unique;const keyword=filter.trim().toLocaleLowerCase("zh-CN");return unique.filter(item=>item.toLocaleLowerCase("zh-CN").includes(keyword))},[filter,options]);const isNewValue=Boolean(value.trim())&&!options.some(item=>item===value.trim());return <div className="combo-input" ref={root}><div className="combo-control"><input value={value} placeholder={placeholder} role="combobox" aria-autocomplete="list" aria-expanded={open} aria-controls={listId} onFocus={()=>{setFilter(null);setOpen(true)}} onChange={event=>{onChange(event.target.value);setFilter(event.target.value);setOpen(true)}} onKeyDown={event=>{if(event.key==="Escape")setOpen(false);if(event.key==="ArrowDown"){event.preventDefault();setFilter(null);setOpen(true)}}}/><button type="button" aria-label="展开选项" aria-expanded={open} onClick={()=>{setFilter(null);setOpen(current=>!current)}}><ChevronDown size={16}/></button></div>{open&&<div className="combo-options" id={listId} role="listbox">{visible.map(item=><button type="button" role="option" aria-selected={item===value} key={item} onPointerDown={event=>event.preventDefault()} onClick={()=>{onChange(item);setFilter(null);setOpen(false)}}><span>{item}</span>{item===value&&<Check size={15}/>}</button>)}{isNewValue&&<div className="combo-new">保存后记住“{value.trim()}”</div>}{!visible.length&&!isNewValue&&<div className="combo-empty">暂无可选项，可直接输入新内容</div>}</div>}</div>}
