> v0.2.2 新增工作日历与应用内手动检查更新，可按周、双周、月回看真实办理轨迹，并在用户主动触发时安全获取 GitHub Releases 最新正式安装包。

---

## 版本亮点

- **真实工作轨迹集中回看**：工作日历支持周、双周、月三种视图；既可用轻量甘特查看跨日与多轮办理，也可按日查看处理、完成数量、事项摘要、工作热度和日期明细，且不补造历史事件。
- **历史记录自动同步**：办理记录作废后不再计入结果，事项合并后继续按主事项展示迁移历史，现有统计口径保持不变。
- **手动检查正式更新**：仅在“关于”页面主动检查时访问 GitHub Releases，下载完成并通过 SHA-256 完整性校验后才启动安装程序。
- **发布流程分层**：开发 Artifact、Beta / RC 和正式 Release 分开构建，正式安装包名称与应用内更新器保持一致。

## 新增

- 侧栏新增“工作日历”，统一提供周、双周、月三种视图，以及真实时间比例甘特、多轮与当前活动区间、每日办理统计、事项摘要、工作热度、日期明细和五种排序方式。
- 关于页面新增手动“检查更新”，独立进度窗口展示真实下载大小、百分比和 SHA-256 校验状态。

## 优化

- 工作日历响应办理、重新入队、合并、归档、删除及恢复事件并自动刷新；被作废的办理记录不计入有效结果和日历工作量，真实队列区间继续保留。
- 更新包只保存到系统临时目录，不写入用户下载目录、桌面、安装目录或 In Line 数据目录。
- 安装器写入文件前再次处理可能被 AI 客户端重启的 `in-line-mcp.exe` 伴随进程，不关闭客户端本体。

## 数据与更新说明

> [!NOTE]
> v0.2.2 继续使用 `%APPDATA%\in-line\inline.db`，不会主动删除现有事项、办理记录或软件设置。工作日历不新增数据库表，也不改变统计中心口径。升级前仍建议在软件设置中手动创建一次备份。

> [!NOTE]
> 应用内检查更新只识别 GitHub Releases 的最新正式版本，忽略 Draft 和 Pre-release。v0.2.2 是首个带该功能的正式版，安装后可用于发现后续正式版本。

> [!WARNING]
> 当前 Windows 安装包尚未进行代码签名，首次安装时可能出现 Windows SmartScreen 提示。请仅从本项目的 GitHub Releases 页面下载安装。

## 下载

| 平台 | 架构 | 安装包 |
| --- | --- | --- |
| Windows 10 / 11 | x64 | `In.Line_0.2.2_x64-setup.exe` |

**完整变更日志**：[v0.2.1...v0.2.2](https://github.com/bluntvoice/In-Line/compare/v0.2.1...v0.2.2)
