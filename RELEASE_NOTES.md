> v0.2.2 新增只读工作日历与关于页面手动检查更新，用真实入队区间还原工作轨迹，并在用户主动触发时安全获取新版安装包。

---

## 版本亮点

- **周工作轨迹一目了然**：同一事项保持一行，每次真实“入队 → 本轮结束”显示为独立甘特区间。
- **多轮处理可追溯**：每段区间显示真实历史轮次，已处理与已完成用不同终点颜色区分。
- **月度节奏更清晰**：传统月历按日展示处理、完成数量、事项摘要和轻量工作热度。
- **历史自动同步**：办理记录作废后不再计入结果，事项合并后继续按主事项展示迁移历史。
- **不改变原有数据体系**：不新增日历表，不修改统计中心口径，继续兼容 v0.1.1 以来的队列与办理记录。
- **手动检查正式更新**：只有用户在“关于”页面点击按钮时才访问 GitHub Releases；安装包经 SHA-256 完整性校验后才启动 NSIS 安装程序。

---

## 新增

- 关于页面新增手动“检查更新”，使用独立右下角窗口显示真实下载与校验进度。
- 更新包保存到系统临时目录，不写入用户下载目录、桌面、安装目录或 In Line 数据目录。
- 侧栏新增一级“工作日历”，默认周视图并支持周 / 月切换、前后周期和回到今天。
- 周视图新增真实时间比例甘特、多轮区间、当前活动区间、周末按需显示和五种排序方式。
- 月视图新增每日办理统计、最多三条事项摘要、工作热度与日期明细抽屉。
- 新增工作日历专用只读聚合接口，避免前端逐事项查询和拼接复杂历史。

## 变更

- 工作区间主体使用中性色，终点按有效办理结果标记；待材料、待内部确认和待对方确认仍保留真实状态文案。
- 当前活动区间延伸至查询时刻并使用渐隐开口；等待期间保持空白，不绘制虚线或人工连接。
- 被作废的办理记录不计入有效结果和日历工作量，对应真实 queue entry 不被伪造或删除。
- 工作日历响应现有 `data-changed` 事件，办理、重新入队、合并、归档、删除及恢复后自动刷新。
- 侧栏回收站入口取消数量徽标，具体数量继续在回收站页面中显示。

## 兼容性

- 不新增数据库 schema 或 migration，继续读取现有 `task_queue_entries` 与 `task_work_events`。
- 旧版迁移数据只展示数据库中已经存在的可靠时间，不补造不存在的日历事件。

---

> [!NOTE]
> v0.2.2 继续使用 `%APPDATA%\in-line\inline.db`，不会主动删除现有事项、办理记录或软件设置。升级前仍建议在软件设置中手动创建一次备份。

> [!WARNING]
> 当前 Windows 安装包尚未进行代码签名，首次安装时可能出现 Windows SmartScreen 提示。请仅从本项目的 GitHub Releases 页面下载安装。

---

正式安装包发布后将在 GitHub Releases 页面提供。

---

**完整变更日志**：[v0.2.1...HEAD](https://github.com/bluntvoice/In-Line/compare/v0.2.1...HEAD)
