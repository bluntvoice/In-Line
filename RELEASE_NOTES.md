> 调整统计中心工作量趋势的日期排版，并增加中文星期标识。

## 优化

- 每日趋势的日期上方显示“周一”“周二”等中文星期，日期与星期采用固定两行布局。
- 数量、柱体和日期标签使用固定轨道，所有柱体从同一基线向上生长。
- 放大趋势图数量、星期、日期与底部说明文字，并利用卡片高度压缩无效留白。
- 事项详情顶部不再重复显示永久事项编号，保留信息区中的统一展示。
- 加急事项的取号码牌改用红色背景，并保留原有“加急”标识。

## 新增

- 软件设置新增“统计比例口径”，默认选择有效处理率，并可切换为事项办结率。
- 事项办结率按“已完成事项 ÷ 周期内实际处理事项”计算。
- 有效处理率按“有效办理事项 ÷ 周期内应处理事项”计算，用于观察本周期仍有多少事项尚未处理。

## 修复

- 取号图片底部中文提示统一使用无衬线字体，与右侧信息标签保持一致；事项编号数字继续使用等宽字体。
- 修复事项数量较多、柱体较高时，日期标签被向下挤压而与其他日期错位的问题。
- 修复窗口状态恢复导致主界面、悬浮窗和新增取号窗口在启动时同时出现的问题。
- 软件启动时仅显示主界面；关闭主界面后默认显示悬浮窗，新增取号窗口仅在用户主动操作时打开。
- 趋势卡片不再随左侧事项类型列表等高拉伸，修复窗口未最大化时柱状图上方空白过大的问题。

## 升级提示

v0.1.3 当前为测试版。升级后继续使用 `%APPDATA%\in-line\inline.db` 中的原有数据，建议安装前在软件设置中手动创建一次备份。

## 下载

| 平台 | 架构 | 文件 |
|------|------|------|
| Windows 10 / 11 | x64 | `In Line_0.1.3_x64-setup.exe` |

**完整变更日志**：https://github.com/bluntvoice/In-Line/compare/v0.1.2...HEAD
