> 新增结构化处理活动、可重复排队和统计中心，部门 / 团队支持多选。

## Highlights

- **处理过程可统计**：通过“本轮已处理”“本轮已完成”或补录处理活动记录每一轮工作，并保留可审计时间线。
- **事项可以再次排队**：已处理、暂缓、已完成和已归档事项可重新加入今日队列；事项编号和历史不变，今日序号重新生成。
- **统计口径更稳定**：按事项去重，以区间内最新处理结果归类，并可查看事项类型明细和日 / 周趋势。
- **部门支持多选**：部门 / 团队现在与对接人一样支持多项选择，旧数据无需手动转换。

## 新增

- “已处理”状态与结构化处理活动。
- 上周、上月、上季度和自定义区间统计。
- 处理结果卡片、完成率、事项类型明细和趋势。
- 已完成 / 已归档事项“重新开启并加入今日队列”。
- 部门 / 团队最多 10 项多选。

## 变更

- 每次重新排队生成新的今日序号，但永久事项编号与处理历史保持不变。
- 重新排队时截止时间默认不继承，可按需勾选保留。
- 修改或作废最早有效处理活动时提示其对历史统计的影响。
- 数据库升级至 schema v5，并在迁移前自动备份。

## 修复

- 测试安装包现在会自动安装 x64 `WebView2Loader.dll`，修复启动时提示缺少该文件的问题。
- 悬浮窗不再出现号码与标题重叠、Windows 11 顶部多余黑线。
- 列表不再显示永久事项编号和非活动队列的“最近序号”，永久编号保留在详情页。
- 详情页处理区改为“办理记录”，事项时间线中的状态统一显示中文。

> [!WARNING]
> v0.1.1 是功能迭代版本，稳定后的统计版本再提升为 v0.2.0。升级会继续使用 %APPDATA%\in-line\inline.db，首次启动会自动迁移旧数据；建议仍保留现有备份。

已在本地生成 v0.1.1 测试安装包，尚未发布到公开 Release。
