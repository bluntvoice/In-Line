> v0.1.4 统一暂缓队列排序、统计事项类型口径和各事项列表的点击行为。

## 版本亮点

- **暂缓事项更容易追踪**：最近从非暂缓状态进入暂缓队列的事项显示在最上方。
- **事项类型统计更准确**：统计中心按照事项当前类型归类，历史办理类型快照仍保留用于审计。
- **列表操作更统一**：单击事项默认查看详情，只有复制按钮生成取号图片。

## 优化

- 暂缓队列按照最近进入时间倒序排列；待补材料、待内部确认、待对方确认、已暂停和已处理之间相互切换时不会重复置顶。
- 有效处理率的“应处理事项”使用当前截止时间过滤：截止时间不在统计周期内、且本周期没有实际办理记录的提前排队事项不再计入；当前截止时间为空时不追溯旧截止时间。
- 主界面的待办、暂缓、历史归档和回收站列表，单击事项统一打开详情。
- 展开模式悬浮窗单击事项打开主界面详情，右侧复制按钮继续用于生成取号图片。
- 迷你悬浮窗保持现有单击复制行为，不增加额外按钮。

## 修复

- 修复事项修改类型后，统计中心仍按照办理记录中的旧类型快照分类的问题。
- 事项类型分布和点击类型后的事项明细均改为使用事项当前类型，避免分组与详情结果不一致。
- 修复主界面停留在软件设置、关于或统计中心时，从展开模式悬浮窗点击事项无法显示详情的问题；现在会自动切换到事项所属队列并打开详情。

## 数据兼容

v0.1.4 继续使用 `%APPDATA%\in-line\inline.db`，不改变事项和办理记录内容。暂缓进入时间由现有状态历史计算，无需用户手动迁移数据。建议安装测试版前在软件设置中创建一次备份。

## 测试安装包

| 平台 | 架构 | 文件 |
|------|------|------|
| Windows 10 / 11 | x64 | `In Line_0.1.4_x64-setup.exe` |

**完整变更日志**：https://github.com/bluntvoice/In-Line/compare/v0.1.3...HEAD
