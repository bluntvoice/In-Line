> v0.2.0 将 In Line 从本地事项队列扩展为可安全接入 AI 的工作记录与报告工具，同时完善统计分析和备份恢复体验。

---

> [!NOTE]
> MCP 仍然只在本机通过 stdio 运行，不监听网络端口，也不会让 In Line 自动联网。AI 工具只能读取用于报告的必要数据，不能修改事项；联系人、事项详情、内部备注、普通操作日志和回收站内容不会通过 MCP 返回。

## 版本亮点

- **AI 报告直接生成**：通过本地只读 MCP 接入 Codex 等工具，统计中心可按当前时间范围生成周报、月报、季度报告等完整指令。
- **统计中心更加灵活**：新增周期及部门 / 团队相关工作量对比，图表可排序、显示或隐藏，事项类型可切换列表与饼状图。
- **备份恢复更安全**：支持导入、刷新和打开备份目录；恢复时合并一致数据、保留冲突数据，并立即提醒需要复核的事项。
- **升级过程更顺畅**：安装器会处理仍被 AI 客户端占用的 MCP 伴随程序，避免更新时必须选择“忽略”。

---

## 新增

- 安装包内置 `in-line-mcp.exe`，为 Codex 等支持 stdio MCP 的 AI 工具提供只读报告数据。
- 软件设置新增“AI MCP 接入”通用指令，可核对本机启动路径后复制给目标客户端完成配置。
- 统计中心新增“生成报告”，支持工作复盘、精简汇报、事项分类和自定义模板，并自动带入报告类型与统计日期。
- 新增周期工作量、部门 / 团队相关工作量对比，以及可持久保存的图表排序和显示设置。
- 事项类型分布新增饼状图模式，列表和饼图均可展开对应事项明细。
- 可恢复备份新增导入、手动刷新和“备份目录”，页面打开期间也会自动发现新加入的备份。

## 变更

- 备份恢复改为非破坏性合并：完全一致的事项合并办理记录，同名但详情不同的事项作为冲突副本保留，恢复前自动创建应急备份。
- 事项类型在备份合并时自动去除首尾空格并按名称去重，软件设置和 Windows 开机启动状态随备份恢复。
- 冲突副本显示持久的“导入冲突”标识；用户可打开详情合并事项，或在人工确认后解除标识。
- 同期比较图表统一使用“红色为增长、绿色为减少”，本周期数值随变化方向着色。
- 图表按行自动对齐，并根据可用空间调整柱体、条目和列表尺寸；事项明细支持再次点击或使用按钮收起。
- 数据库升级至 schema v7，用于保存导入冲突复核状态。

## 修复

- 修复恢复备份后可能没有响应、主界面数据未及时刷新的问题。
- 修复导入备份后，已处理、已完成等非待办事项可能错误进入待办队列的问题。
- 修复 MCP 程序被 Codex 等客户端占用时，安装更新包无法覆盖文件的问题。
- 修复同名冲突事项只能依靠标题后缀事后查找、缺少立即提醒和持久标识的问题。
- 修复事项类型饼状图展开明细时，因两阶段布局更新导致页面闪烁的问题。

---

> [!NOTE]
> v0.2.0 继续使用 `%APPDATA%\in-line\inline.db`。首次升级数据库前会自动创建迁移备份，原有事项和办理记录不会被主动删除。升级前仍建议在软件设置中手动创建一次备份。

> [!WARNING]
> 当前 Windows 安装包尚未进行代码签名，首次安装时可能出现 Windows SmartScreen 提示。请仅从本项目的 GitHub Releases 页面下载安装。

---

## 下载

| 平台 | 架构 | 安装包 |
|------|------|--------|
| Windows 10 / 11 | x64 | [In.Line_0.2.0_x64-setup.exe](https://github.com/bluntvoice/In-Line/releases/download/v0.2.0/In.Line_0.2.0_x64-setup.exe) |

---

**完整变更日志**：[v0.1.4...v0.2.0](https://github.com/bluntvoice/In-Line/compare/v0.1.4...v0.2.0)
