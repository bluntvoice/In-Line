> v0.3.0 重做备份导入与恢复体验：一次完成校验、导入、安全备份和非破坏性合并，并让成功结果、失败原因和后续操作清楚可见。

---

## 版本亮点

- **一次完成导入与恢复**：在软件设置中选择外部 `.db` 后，一次确认即可完成完整性校验、复制、安全备份和非破坏性合并，不再需要导入后寻找圆形图标执行第二步。
- **恢复结果清楚可见**：右下角通知明确展示新增、合并和冲突数量，并可直接跳转待办、暂缓或历史归档查看数据。
- **失败可以自行处理**：通知显示实际失败原因和对应建议；如果文件已导入但恢复失败，可直接点击“重试恢复”。
- **大量备份也不隐藏**：备份列表显示全部记录和总数，新导入项固定置顶并高亮，单条恢复按钮使用明确文字。

## 新增

- 新增空数据库恢复提示，说明选择备份后会自动恢复且仍会创建安全备份。
- 新增导入排序与失败建议自动化测试，并记录空库恢复、大量备份与页面跳转的隔离验收结果。

## 修复

- 修复外部备份导入成功后仍需执行隐蔽的第二步操作，用户容易误认为数据没有导入的问题。
- 修复界面只显示前 12 条备份，历史记录较多时导入项可能不可见的问题。
- 修复 Windows 复制外部备份时保留源文件旧修改时间，导致新导入项没有排在列表顶部的问题。

## 数据安全与兼容性

> [!NOTE]
> v0.3.0 继续使用 `%APPDATA%\in-line\inline.db`，不新增数据库 schema 或 migration。备份恢复仍采用事务内非破坏性合并，操作前自动生成 `before-restore` 应急备份。

> [!NOTE]
> 当前数据库已有数据不会阻止正常导入：完全相同事项合并记录，同名不同内容保留为冲突副本，编号重复自动重新分配。备份中的有效软件设置仍会覆盖当前同名设置。

> [!WARNING]
> 当前 Windows 安装包尚未进行代码签名，首次安装时可能出现 Windows SmartScreen 提示。请仅从本项目 GitHub Releases 页面下载安装。

## 下载

| 平台 | 架构 | 安装包 |
| --- | --- | --- |
| Windows 10 / 11 | x64 | `In.Line_0.3.0_x64-setup.exe` |

**完整变更日志**：[v0.2.2...v0.3.0](https://github.com/bluntvoice/In-Line/compare/v0.2.2...v0.3.0)
